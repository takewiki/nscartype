menu_column <- tabItem(tabName = "column",
                       fluidRow(
                         column(width = 12,
                                box(
                                  title = "品牌车型修改", width = NULL, solidHeader = TRUE, status = "primary",
                                  'aa'
                                )
                         ))
)